{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install -r requirements.txt && pip install --no-deps . && npm install --prefix frontend",
  "functions": {
    "backend/index.py": {
      "maxDuration": 300
    }
  }
}
